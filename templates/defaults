#подсеть для контейнеров
NET="192.168.XX"

#название сети в docker
N_NAME="nodes"

#локальный мост, к которому подключаются контейнеры
IFACE=br0

#название создаваемого macvlan интерфейса на хосте
NET_IFACE="${IFACE}-nds"

#адрес хоста на котором все это делается
HOST="${NET}.YYY"

#шлюз для выхода в интернет (обычно совпадает с хостом)
GW=$HOST


